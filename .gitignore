# Runtime data, secrets, and logs
# /config/ holds the live SQLite database (with API credentials), worker.log and worker.lock
/config/

# SQLite artifacts (safety net, in case databases appear outside /config/)
*.sqlite
*.sqlite-*

# Logs and lock files
*.log
*.lock

# Editor and operating-system files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.swp
*~

# Internal planning notes (not for the public repo)
plan.md
